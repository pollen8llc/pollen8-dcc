
import React from "react";

/**
 * @deprecated This component is maintained for backward compatibility.
 * Community functionality has been removed from the platform.
 */
const PlexusBackground: React.FC = () => {
  // Return an empty div since this is just a visual component
  return <div className="absolute inset-0 z-0 opacity-20"></div>;
};

export default PlexusBackground;
